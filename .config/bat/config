# bat (alias cat) configuration

# colorful
--theme="Dracula"
# header might be nice
--style="changes"
# with TERM=tmux-256color and a modern ncurses first in path, italics will work in iTerm2
--italic-text=always
# remove first column, it confuses bat. Also, in cat, quit more easily
--pager="less -+W -+J --QUIT-AT-EOF --no-init"
